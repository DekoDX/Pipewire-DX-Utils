stream.properties = {
    resample.quality = 10
    channelmix.normalize  = false
    channelmix.mix-lfe    = false
    channelmix.upmix      = false
    channelmix.upmix-method = psd # Better upmixing in the case you want to enable it
    channelmix.lfe-cutoff = 0
    channelmix.fc-cutoff  = 0
    channelmix.rear-delay = 0 # Set this to the default of 12.0 if using speakers instead of virtual surround
    channelmix.stereo-widen = 0.0
    channelmix.hilbert-taps = 0
    dither.noise = 0
}
